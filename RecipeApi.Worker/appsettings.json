{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.EntityFrameworkCore": "Warning",
      "System.Net.Http.HttpClient": "Warning"
    }
  },

  "//ConnectionStrings": "Local development default, matching docker-compose.yml. Production supplies its own through ConnectionStrings__Postgres in the environment.",
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5433;Database=recipefinder;Username=recipeapi;Password=localdev"
  },

  "Scraping": {
    "//AllowLoopbackHosts": "Must stay false. Override with Scraping__AllowLoopbackHosts=true only to crawl a local fixture server.",
    "AllowLoopbackHosts": false,
    "PolitenessDelaySeconds": 1.0,
    "TimeoutSeconds": 15,
    "MaxResponseBytes": 2097152,
    "//ParserVersion": "Raise this after changing the normalizer or mapper, then run `promote --repromote` to re-transform the staged corpus without re-crawling.",
    "ParserVersion": 1
  }
}
